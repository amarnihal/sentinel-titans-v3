# Sentinel Titans — website

Scaffolded Next.js + TypeScript + Tailwind project. This initial scaffold contains a single blank white page at `/`.

To get started:
- Run `npm install`
- Run `npm run dev` to start the dev server

Note: I did not push or initialize a git remote as requested.
